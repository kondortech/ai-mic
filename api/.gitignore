# Dependencies
node_modules/

# npm / Node
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Local env (never commit secrets)
.env
.env.*
!.env.example

# Legacy / script output under api/ (Makefile uses repo-root paths; npm scripts may still write here)
generated/

# OS / editor
.DS_Store
